<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SIMTICAF.Vistas.Categorias"
             BackgroundColor="Gray">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="Center" Margin="20, 0, 20, 20" >
            <Label Text="Categorías" FontSize="35" Margin="20" VerticalOptions="Center"  TextColor="Yellow" WidthRequest="100" HeightRequest="60" HorizontalTextAlignment="Center"/>
            <Button Text="Agregar" WidthRequest="100" HeightRequest="40" BackgroundColor="BurlyWood" TextColor="Black" Clicked="Btn_AgregarCategoria"/>

            <ListView x:Name="ListaCategorias" BackgroundColor="LightGray">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding descripcion}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>